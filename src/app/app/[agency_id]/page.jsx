'use client';
import Header from '@/app/components/Header';
import React from 'react';

export default function AgencyHomePage({ params }) {
  const agency_id = params?.agency_id || null;
  console.log("Agency ID:,rmemrememrm", agency_id);

  return (
    <>
     
    </>
  );
}
